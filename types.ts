export interface BlogSectionData {
  id: string;
  title: string;
  content: string;
  imageUrl: string | null;
  imagePrompt: string;
}

export interface OutlineSection {
    title: string;
    imagePrompt: string;
}
